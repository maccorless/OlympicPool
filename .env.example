# Flask Configuration
FLASK_SECRET_KEY=dev-secret-key-for-local-testing-only
FLASK_DEBUG=True
BASE_URL=http://localhost:5000

# Admin Emails (comma-separated)
# Contest-level admins - can manage specific contests
ADMIN_EMAILS=your@email.com

# Global Admins (comma-separated) - can create/manage events and contests
# If not set, falls back to ADMIN_EMAILS
GLOBAL_ADMIN_EMAILS=your@email.com

# SMS Authentication (Twilio) - Required for production
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
NO_SMS_MODE=True

# Email Service (Resend) - Optional for magic links
RESEND_API_KEY=
NO_EMAIL_MODE=True

# Session Security (set to True in production with HTTPS)
SESSION_COOKIE_SECURE=False

# Database Location (optional - defaults to instance/)
# DATABASE_DIR=instance

# Production Notes:
# - Generate a strong SECRET_KEY for production (min 32 chars)
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# - Set FLASK_DEBUG=False in production
# - Set SESSION_COOKIE_SECURE=True in production (requires HTTPS)
# - Configure Twilio credentials and set NO_SMS_MODE=False for SMS OTP
# - Add your RESEND_API_KEY and set NO_EMAIL_MODE=False for real emails (optional)
# - Update BASE_URL to your production domain (https://medalpool.com)
# - On Railway, DATABASE_DIR should be /app/instance (matched to volume mount)
